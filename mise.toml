[tools]
uv = "0.11.21"
ruff = "0.16.8"
ty = "0.0.81"


[settings]
experimental = true

[env]
UV_PYTHON = "3.14"

[tasks.lint]
description = "Run ruff linter"
run = "ruff check ."

[tasks.format]
description = "Format code with ruff"
run = "ruff format ."

[tasks."format:check"]
description = "Check formatting without modifying files"
run = "ruff format --check ."

[tasks.typecheck]
description = "Run ty type checker"
run = "ty check ."

[tasks.test]
description = "Run pytest"
run = "uv run pytest -q"

[tasks.check]
description = "Run all checks (lint, format, typecheck, test)"
run = """
ruff check .
ruff format --check .
ty check .
uv run pytest -q
"""

[tasks.dev]
description = "Start MCP HTTP server with auto-reload on code changes"
run = """
uv run watchmedo auto-restart \
  --directory mcp_bauplan \
  --pattern '*.py;*.md' \
  --recursive \
  -- python main.py --transport streamable-http
"""
